target:
  url: "https://example.com"
  method: "GET"

load:
  total_requests: 10
  concurrency: 2
  timeout_seconds: 30
  ramp_up_seconds: 0

assertions:
  expected_status: [200]
  max_p95_ms: 2000
  max_error_rate_percent: 0

report:
  output_dir: "reports"
  name: "simple-get"
