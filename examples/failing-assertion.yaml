target:
  url: "https://example.com"
  method: "GET"

load:
  total_requests: 5
  concurrency: 1
  timeout_seconds: 30
  ramp_up_seconds: 0

assertions:
  expected_status: [201]
  max_error_rate_percent: 0

report:
  output_dir: "reports"
  name: "failing-assertion"
